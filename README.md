# PathFinder (Offline Demo Build)

This version is **offline-only**: zero network / API calls.

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

Environment:
- `PATHFINDER_OFFLINE=1` (default)
